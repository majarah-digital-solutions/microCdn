version: '3'
services:
  microcdn:
    container_name: microCdn
    build: 
      context: ../../.
      target: development
    restart: always
    ports:
      - 15000:15000
    volumes:
      - ../../src:/microCdn/src:ro
      - ../../public:/microCdn/public
    env_file:
      - ./.env